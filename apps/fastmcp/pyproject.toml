[project]
name = "coelho-search-mcp-fastmcp"
version = "1.0.0"
description = "COELHO Server MCP - FastMCP server (Model Context Protocol tool surface)"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.2,<4.0",
    # [standard] adds uvloop/httptools/watchfiles for --reload-include in entrypoint.sh.
    "uvicorn[standard]>=0.32,<1.0",
    "httpx>=0.28,<1.0",
    "pydantic>=2.9,<3.0",
    "tavily-python>=0.5,<1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = []
